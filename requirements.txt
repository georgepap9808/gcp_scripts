# Python Requirements for Data Lake Platform Scripts
# Install with: pip install -r requirements.txt

# PySpark - Apache Spark Python API
# Version should match your Spark version (3.5.0 in this project)
pyspark>=3.5.0

# Delta Lake - ACID transactions for Spark
delta-spark>=3.1.0

# Google Cloud Storage - For GCS bucket access
google-cloud-storage>=2.10.0

# Optional but recommended for better performance
numpy>=1.21.0
pandas>=1.3.0

# Trino - SQL query engine for data lakes
trino>=0.327.0

# Note: Apache Hudi and Apache Iceberg JARs are loaded via Spark config, not pip packages
# - Hudi: org.apache.hudi:hudi-spark3.5-bundle_2.12:0.15.0 (loaded via spark.jars.packages)
# - Iceberg: org.apache.iceberg:iceberg-spark-runtime-3.5_2.12:1.4.3 (loaded via spark.jars.packages)
# - Kafka: org.apache.spark:spark-sql-kafka-0-10_2.12:3.5.0 (loaded via spark.jars.packages)
# - PostgreSQL JDBC: org.postgresql:postgresql:42.5.0 (loaded via spark.jars.packages)
